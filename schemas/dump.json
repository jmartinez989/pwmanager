{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "title": "Dump Decrypted Passwords File with Passwords in Clear Text",
    "properties": {
        "key": {
            "type": "string",
            "description": "Key to use for encryption/decryption"
        },
        "action": {
            "type": "integer",
            "description": "Integer for action to take."
        },
        "fileDir": {
            "type": "string",
            "description": "Directory of where to dump the file. Filename will be pwdump.json. This value can also be 'stdout' to display to the screen but be careful using that."
        }
    },
    "required": ["key", "action", "fileDir"]
}
